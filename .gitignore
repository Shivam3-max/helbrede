# dependencies
node_modules

# next.js build output
.next
out
*.tsbuildinfo

# runtime SQLite database (auto-seeds from src/data/products.json on first run)
data/*.db
data/*.db-shm
data/*.db-wal

# user-uploaded product images (runtime state)
public/uploads/*
!public/uploads/.gitkeep

# env & secrets
.env*

# misc
.DS_Store
*.log
